# Migration `20201118211107-init`

This migration has been generated by David Velandia at 11/18/2020, 4:11:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "citizenshipCard" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "lastname" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL
)

CREATE TABLE "Account" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "accountType" TEXT NOT NULL,
    "statement" TEXT NOT NULL,
    "availableValue" REAL NOT NULL,
    "clientId" INTEGER NOT NULL,

    FOREIGN KEY ("clientId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Movement" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "clientId" INTEGER NOT NULL,
    "accountId" INTEGER NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "amount" REAL NOT NULL,
    "type" TEXT NOT NULL,
    "description" TEXT NOT NULL,

    FOREIGN KEY ("clientId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("accountId") REFERENCES "Account"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "User.citizenshipCard_unique" ON "User"("citizenshipCard")

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201118211107-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,42 @@
+datasource db {
+  provider = "sqlite"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id                Int      @id @default(autoincrement())
+  citizenshipCard   String   @unique
+  name              String
+  lastname          String
+  email             String   @unique
+  password          String
+  accounts          Account[]
+  movements         Movement[]
+}
+
+model Account {
+  id                Int         @id @default(autoincrement())
+  createdAt         DateTime    @default(now())
+  accountType       String
+  statement         String
+  availableValue    Float
+  client            User        @relation(fields: [clientId], references: [id])
+  clientId          Int
+  movements         Movement[]
+}
+
+model Movement {
+  id                Int         @id @default(autoincrement())
+  client            User        @relation(fields: [clientId], references: [id])
+  clientId          Int
+  account           Account     @relation(fields: [accountId], references: [id])
+  accountId         Int
+  createdAt         DateTime    @default(now())
+  amount            Float
+  type              String
+  description       String
+}
```


